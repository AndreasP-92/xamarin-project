
@model IEnumerable<XamarinProjectBackend.Models.CoordInfo>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="col-6">
        <h2 class="text-primary">Liste af koordinater</h2>
    </div>
    <div class="col-12 text-right">
        <a class="btn btn-success" asp-controller="CoordInfo" asp-action="Create">Opret nyt koordinat</a>
        @*<a class="btn btn-success" asp-controller="CoordInfo" asp-action="ImportJsonFile">Importér JSON Fil</a>*@
    </div>
    <br />
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    id
                </th>
                <th>
                    Navn
                </th>
                <th>
                    Beskrivelse
                </th>
                <th>
                    Type
                </th>
                <th>
                    Latitude
                </th>
                <th>
                    Longitute
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
                { 
                    <tr>
                        <td width="25%">@item.Id</td>
                        <td width="25%">@item.Name</td>
                        <td width="25%">@item.Description</td>
                        <td width="25%">@item.Type</td>
                        <td width="25%">@item.Lat</td>
                        <td width="25%">@item.Lon</td>
                        <td width="25%">
                            <a class="btn btn-primary w-100 mb-3" asp-controller="Coordinfo" asp-action="Update" asp-route-Id="@item.Id">Update</a>
                            <a class="btn btn-danger w-100" asp-controller="Coordinfo" asp-action="Delete" asp-route-Id="@item.Id">Delete</a>

                        </td>
                    </tr>
                }
        </tbody>
    </table>
</div>

